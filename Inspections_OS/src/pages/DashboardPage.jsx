import { localDb } from '@/lib/localDb';
export default function DashboardPage() { const projects = localDb.listProjects(); const runs = localDb.listRuns(); const issues = localDb.listIssues(); const exports = localDb.listExports(); return <div className="grid" style={{ gap: 20 }}><div><h1 className="title">Clean Wizard / Gate / Export Handoff</h1><p className="subtitle">Focused reproduction of the abstraction layer we developed in this chat, rebuilt without generated entity clients or hosted-platform auth dependencies.</p></div><div className="grid grid-2"><div className="card"><div className="label">What this package proves</div><ul className="small"><li>Wizard creation flow resolves from project + program + stage.</li><li>Gate evaluation uses rule-driven requirement checks.</li><li>Exports generate manifest-backed PDF packets.</li><li>Local repository pattern is swappable with a real backend later.</li></ul></div><div className="card"><div className="label">Current local state</div><table className="table"><thead><tr><th>Entity</th><th>Count</th></tr></thead><tbody><tr><td>Projects</td><td>{projects.length}</td></tr><tr><td>Runs</td><td>{runs.length}</td></tr><tr><td>Issues</td><td>{issues.length}</td></tr><tr><td>Exports</td><td>{exports.length}</td></tr></tbody></table></div></div></div>; }
