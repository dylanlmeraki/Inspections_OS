import { localDb } from '@/lib/localDb';
export default function ExportCenterPage() { const exports = localDb.listExports(); const manifests = localDb.listManifests(); return <div><h1 className="title">Export Center</h1><p className="subtitle">Manifest-backed export jobs created by the cleanroom export engine.</p><div className="grid grid-2"><div className="card"><div className="label">Export jobs</div>{exports.length === 0 ? <p className="small">No exports yet.</p> : <table className="table"><thead><tr><th>Title</th><th>Packet</th><th>Rule Snapshot</th></tr></thead><tbody>{exports.map((exp) => <tr key={exp.id}><td>{exp.title}</td><td>{exp.packetClass}</td><td><span className="code">{exp.ruleSnapshotId}</span></td></tr>)}</tbody></table>}</div><div className="card"><div className="label">Verification manifests</div>{manifests.length === 0 ? <p className="small">No manifests yet.</p> : manifests.map((m) => <div className="list-item" key={m.id}><strong>{m.stageCode}</strong><div className="small">{m.jurisdictionKey} · {m.inspectionTypeCode}</div><div className="small">Sources: {m.sourceEntries.map((s) => s.title).join('; ')}</div></div>)}</div></div></div>; }
