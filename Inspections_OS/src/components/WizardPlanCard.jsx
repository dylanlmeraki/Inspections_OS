export default function WizardPlanCard({ plan }) {
  if (!plan) return null;
  return <div className="card"><div className="label">Abstraction Layer Result</div><h3 style={{ marginTop: 0 }}>{plan.template.title}</h3><p className="small">{plan.project.name} · {plan.project.countyGroup} · {plan.program.name} · {plan.stage.name}</p><div className="list"><div className="list-item"><strong>Mode</strong><div className="small">{plan.mode}</div></div><div className="list-item"><strong>Required fields</strong><div className="small">{plan.requiredFieldKeys.join(', ') || 'None'}</div></div><div className="list-item"><strong>Required questions</strong><div className="small">{plan.requiredQuestionKeys.join(', ') || 'None'}</div></div><div className="list-item"><strong>Required documents</strong><div className="small">{plan.requiredDocumentKeys.join(', ') || 'None'}</div></div><div className="list-item"><strong>Manifest source records</strong><div className="small">{plan.sourceRecords.map((s) => `${s.title} (${s.verificationStatus})`).join('; ') || 'None'}</div></div></div></div>;
}
