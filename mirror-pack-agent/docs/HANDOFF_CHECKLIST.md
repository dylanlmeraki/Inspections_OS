# Handoff Checklist

Before running a task:
- Place source documents in docs/source_set/
- Confirm the task slice is narrow
- Confirm the collaborator understands: no product code edits

After running a task:
- Run scripts/validate_outputs.py .
- Return the exact task YAML used
- Return the four output files
- Return the logs/<task>.json file
